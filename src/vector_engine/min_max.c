/*
** EPITECH PROJECT, 2020
** csfml_physics
** File description:
** min_max
*/

float minf(float a, float b)
{
    if (a < b) {
        return (a);
    }
    return (b);
}

float maxf(float a, float b)
{
    if (a > b) {
        return (a);
    }
    return (b);
}

int min(int a, int b)
{
    if (a < b) {
        return (a);
    }
    return (b);
}

int max(int a, int b)
{
    if (a > b) {
        return (a);
    }
    return (b);
}